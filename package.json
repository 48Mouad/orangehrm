{
  "name": "orangehrm-tests",
  "version": "1.0.0",
  "description": "Tests automatisés Playwright pour OrangeHRM",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "typescript": "^5.0.0"
  }
}
